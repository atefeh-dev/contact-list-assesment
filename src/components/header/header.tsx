import "./header.scss";

function Header() {
  return (
    <header>
      <nav className="navbar">
        <h1 data-testid="navbar/title">Contact List</h1>
      </nav>
    </header>
  );
}

export default Header;
